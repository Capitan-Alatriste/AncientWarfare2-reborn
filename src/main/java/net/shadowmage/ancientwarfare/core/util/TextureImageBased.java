package net.shadowmage.ancientwarfare.core.util;

public class TextureImageBased {
    // TODO Phase 4 TextureImageBased relies on deleted legacy OpenGL rendering pipelines.
    // It will be migrated strictly during the rendering phase.
}
